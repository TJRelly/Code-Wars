function breakChocolate(n, m) {
    numOfBreaks = m * n - 1
    if (n === 0 || m === 0) numOfBreaks = 0
    return numOfBreaks
}

function breakChocolate(n, m) {
    return (n === 0 || m === 0)
        ? numOfBreaks = 0
        : numOfBreaks = m * n - 1
}